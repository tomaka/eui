use Event;
use Matrix;
use Shape;

fn draw() -> Vec<Shape> {
    vec![]
}

fn set_dimensions(matrix: &Matrix, viewport_height_per_width: f32) -> Vec<Box<Event>> {
    vec![]
}

fn set_cursor(cursor: Option<(f32, f32)>) -> Vec<Box<Event>> {
    vec![]
}
